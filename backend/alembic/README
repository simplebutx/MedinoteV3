Alembic 사용법

DB 모델 수정 시
alembic revision --autogenerate -m "변경 내용"

생성된 파일 확인 -> 문제 없으면

alembic upgrade head

서버 시작 uvicorn app.main:app --reload

------

alembic stamp head
- DB는 실제로 수정하지 않고, Alembic 기록만 최신상태로 표시
- 기존 DB와 Alembic 기록을 처음 연결할 때 쓰는 정리용 명령